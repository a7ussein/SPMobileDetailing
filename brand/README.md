# SP Mobile Detailing — Brand Assets

Drop the final brand-identity outputs here once generated:

- `logo-primary.svg` — primary horizontal lockup
- `logo-mark.svg` — standalone mark / favicon source
- `logo-stacked.svg` — stacked variant
- `logo-mono-black.svg`, `logo-mono-white.svg` — single-color
- `brand-guide.pdf` (or `.html`) — colors, type, usage, voice
- `social/` — OG image, IG profile, IG post template
- `tokens.css` — the exact CSS custom properties to paste into `src/styles/global.css`

Until these exist, the site uses placeholder tokens.
